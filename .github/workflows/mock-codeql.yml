name: Mock CodeQL

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  mock-codeql:
    runs-on: ubuntu-latest
    steps:
      - name: Pretend to run CodeQL
        run: echo "Mock CodeQL passed"
